<div class="navbar navbar-inverse navbar-fixed-top">
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Trash Collector 2.0", "Index", "Home", new { area = "" }, new { @class = "navbar-brand" })
        </div>
        <div class="navbar-collapse collapse">
            <ul class="nav navbar-nav">
                @if (User.IsInRole("TrashCollectorEmployee"))
                {
                    <li>@Html.ActionLink("Employee Menu", "EmployeeMenu", "TrashCollector")</li>
                }
                @if (Request.IsAuthenticated)
                {
                <li>@Html.ActionLink("User Menu", "UserMenu", "RegisteredUser")</li>
                }
            </ul>
            @Html.Partial("_LoginPartial")
        </div>
    </div>
</div>